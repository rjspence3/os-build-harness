You are authoring ONE build unit into an OutSystems app via the Model API. Do exactly this unit and nothing else; do not refactor unrelated screens. On every rendered widget you create, set an HTML attribute data-spec-id="<the component id below>" (OutSystems widget Attributes: Name=data-spec-id, Value=the id) so runtime verification can resolve it exactly; for a data table/list also set data-entity="<EntityName>" on the container and data-row-id on each row. Do NOT add any platform Role to a screen unless this unit is explicitly about auth — keep screens Anonymous. Author DIRECTLY and rely on the final model validation — do NOT run read-only introspection loops to enumerate or 'verify' your own work. In particular, do NOT read a `.Name` PROPERTY off Model-API node/widget/type interfaces: MANY do not expose one and it is a compile error (IMobileWidget, IActionNode, IFlowNode, ITypeSignature, IUIFlowNodeSignature, IIdentifierType, IBasicType all lack `.Name`). If you must name a type use GetInterface().Name; otherwise skip the diagnostic and just author. After authoring, run the model validation and report errors/warnings; do NOT publish (the orchestrator publishes).

On the queue screen, add a screen aggregate over the QualificationCase entity (pin the aggregate name so it is not auto-renamed). Add a Table with component id caseTable (set data-spec-id="caseTable" and data-entity="QualificationCase" on it) bound to that aggregate. Put it in the screen's content area (to the right of the nav). Render EACH column as its specified product-UI cell (not a raw value grid) — the theme's stylesheet paints the chip/badge/tag/avatar/glyph classes below into the real look:
   - `CaseNo`: a monospace id cell — an Expression showing QualificationCase.CaseNo inside a Container whose Style class is "cell-id".
   - `Owner`: a round avatar cell — a Container Style class "avatar" containing an Expression = ToUpper(Substr(QualificationCase.Owner, 0, 2)) (the initials). The theme rounds + colors it.
   - `Tier`: a status tag — an Expression showing QualificationCase.Tier INSIDE a Container whose Style is the EXPRESSION "tag tag-" + ToLower(QualificationCase.Tier) (so ".tag" makes the rounded pill and ".tag-<value>" tints it per the theme). Do NOT hardcode one class.
   - `Stage`: a plain Expression cell showing QualificationCase.Stage.
   - `Status`: a status chip — an Expression showing QualificationCase.Status INSIDE a Container whose Style is the EXPRESSION "chip chip-" + ToLower(QualificationCase.Status) (so ".chip" makes the rounded pill and ".chip-<value>" tints it per the theme). Do NOT hardcode one class.
   - `SlaState`: a status badge — an Expression showing QualificationCase.SlaState INSIDE a Container whose Style is the EXPRESSION "badge badge-" + ToLower(QualificationCase.SlaState) (so ".badge" makes the rounded pill and ".badge-<value>" tints it per the theme). Do NOT hardcode one class.
Set data-spec-id="<field>cell" on each styled cell container so runtime verification resolves it. Each row links to the caseDetail screen passing the record's Id. The table must render the real rows — if it comes up empty, the aggregate binding is wrong; fix it, do not leave an empty table.