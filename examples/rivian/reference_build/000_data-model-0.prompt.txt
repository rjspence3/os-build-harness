You are authoring ONE build unit into an OutSystems app via the Model API. Do exactly this unit and nothing else; do not refactor unrelated screens. On every rendered widget you create, set an HTML attribute data-spec-id="<the component id below>" (OutSystems widget Attributes: Name=data-spec-id, Value=the id) so runtime verification can resolve it exactly; for a data table/list also set data-entity="<EntityName>" on the container and data-row-id on each row. Do NOT add any platform Role to a screen unless this unit is explicitly about auth — keep screens Anonymous. Author DIRECTLY and rely on the final model validation — do NOT run read-only introspection loops to enumerate or 'verify' your own work. In particular, do NOT read a `.Name` PROPERTY off Model-API node/widget/type interfaces: MANY do not expose one and it is a compile error (IMobileWidget, IActionNode, IFlowNode, ITypeSignature, IUIFlowNodeSignature, IIdentifierType, IBasicType all lack `.Name`). If you must name a type use GetInterface().Name; otherwise skip the diagnostic and just author. After authoring, run the model validation and report errors/warnings; do NOT publish (the orchestrator publishes).

Create the app's data model. Author ALL of these entities in THIS ONE turn (they may reference each other, so they must be created together). Do NOT create any screens or UI in this turn.
- Supplier: Code: Text; Name: Text; Tier: Text; Status: Text; Country: Text
- Part: Sku: Text; Name: Text; SupplierId: a foreign-key reference to Supplier; Category: Text; Status: Text
- QualificationCase: CaseNo: Text; SupplierId: a foreign-key reference to Supplier; PartId: a foreign-key reference to Part; Tier: Text; Status: Text; Stage: Text; Score: Text; SlaState: Text; Owner: Text
- ReviewTask: CaseId: a foreign-key reference to QualificationCase; Team: Text; State: Text; DueDate: Text
- AuditEvent: CaseId: a foreign-key reference to QualificationCase; Description: Text; Actor: Text; At: Text
IDENTIFIER — settle it in THIS turn, before any publish (this is load-bearing: the create/edit form's Save action later needs Supplier.CreateAction and .Id, which do not exist if the entity has no identifier). Every entity MUST end this turn with exactly ONE identifier attribute: an auto-number Long-Integer named Id. ODC normally attaches this default Id on entity creation — but do NOT assume it; after authoring, READ each entity back and CONFIRM it has an Id identifier attribute. For ANY entity (Supplier, Part, QualificationCase, ReviewTask, AuditEvent) missing one, explicitly create an auto-number Long-Integer attribute named Id and set it as that entity's identifier. Never leave an entity without an identifier, and never CHANGE an existing identifier (post-first-publish that is irreversible, OS-DPL-RDBS-40020).
After authoring, run model validation AND report each entity's identifier attribute by name (so a silent drop is caught now, not at the first write-path). Do not publish.