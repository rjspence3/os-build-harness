You are authoring ONE build unit into an OutSystems app via the Model API. Do exactly this unit and nothing else; do not refactor unrelated screens. On every rendered widget you create, set an HTML attribute data-spec-id="<the component id below>" (OutSystems widget Attributes: Name=data-spec-id, Value=the id) so runtime verification can resolve it exactly; for a data table/list also set data-entity="<EntityName>" on the container and data-row-id on each row. Do NOT add any platform Role to a screen unless this unit is explicitly about auth — keep screens Anonymous. Author DIRECTLY and rely on the final model validation — do NOT run read-only introspection loops to enumerate or 'verify' your own work. In particular, do NOT read a `.Name` PROPERTY off Model-API node/widget/type interfaces: MANY do not expose one and it is a compile error (IMobileWidget, IActionNode, IFlowNode, ITypeSignature, IUIFlowNodeSignature, IIdentifierType, IBasicType all lack `.Name`). If you must name a type use GetInterface().Name; otherwise skip the diagnostic and just author. After authoring, run the model validation and report errors/warnings; do NOT publish (the orchestrator publishes).

Create ONE reusable Web Block named SidebarNav that renders the app's persistent left app-shell sidebar, so every screen references this single block instead of re-authoring the nav. This is an INTERNAL app-shell block — author it NON-PUBLIC (Public=false; a new block may default to Public=true). CRITICAL: a PUBLIC Web Block whose internal screen action performs a navigation (a DestinationNode, e.g. the logout) trips OS-DPL-50205 'Model features validation failed' at PUBLISH (0 errors in-session) — the platform can't guarantee the target screen exists in a consuming app. Keep this block Public=false. Give the block's root Container the Style class "app-sidebar" (the theme paints the dark shell). The block contains, in order:
   - a brand header (Style class "sidebar-brand") showing "RIVIAN" and a subtitle "Supplier & Parts Onboarding".
   - a section header (Style class "nav-section") "OPERATIONS"
   - a nav item (Container Style class "nav-item", data-spec-id="dashboard") whose link is labelled EXACTLY "Dashboard" and navigates to the dashboard screen with a leading mono tag chip (Container Style class "nav-tag") showing "DSH". When the current screen IS dashboard, add the "is-active" class to that item.
   - a nav item (Container Style class "nav-item", data-spec-id="queue") whose link is labelled EXACTLY "Case Queue" and navigates to the queue screen with a leading mono tag chip (Container Style class "nav-tag") showing "QUE" and a trailing count badge (Container Style class "nav-badge") showing "47". When the current screen IS queue, add the "is-active" class to that item.
   - a nav item (Container Style class "nav-item", data-spec-id="caseDetail") whose link is labelled EXACTLY "Case Detail" and navigates to the caseDetail screen with a leading mono tag chip (Container Style class "nav-tag") showing "CSE". When the current screen IS caseDetail, add the "is-active" class to that item.
   - a nav item (Container Style class "nav-item", data-spec-id="screening") whose link is labelled EXACTLY "Compliance Screening" and navigates to the screening screen with a leading mono tag chip (Container Style class "nav-tag") showing "SCR" and a trailing count badge (Container Style class "nav-badge") showing "3". When the current screen IS screening, add the "is-active" class to that item.
   - a nav item (Container Style class "nav-item", data-spec-id="release") whose link is labelled EXACTLY "Part Release" and navigates to the release screen with a leading mono tag chip (Container Style class "nav-tag") showing "PRT" and a trailing count badge (Container Style class "nav-badge") showing "4". When the current screen IS release, add the "is-active" class to that item.
   - a section header (Style class "nav-section") "SUPPLIER"
   - a nav item (Container Style class "nav-item", data-spec-id="intake") whose link is labelled EXACTLY "Supplier Intake" and navigates to the intake screen with a leading mono tag chip (Container Style class "nav-tag") showing "INT". When the current screen IS intake, add the "is-active" class to that item.
   - a footer user block (Container Style class "sidebar-user") showing "Devin Lang" and role "Tech Lead · AIDLC", with an online dot.
CRITICAL: each link must be a single link whose displayed text is EXACTLY its label and nothing else — OutSystems gives a new Link widget a default literal "link" Text widget; DELETE that literal "link" ITextWidget from each link so it does not render "linkInbox"-style prefixes (a scan of Expression children alone looks clean and misses it — find and remove the literal-text widget). Set data-spec-id on each item = its toScreen id. Do not fan this nav out per screen; this block IS the nav.