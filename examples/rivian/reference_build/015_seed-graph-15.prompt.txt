You are authoring ONE build unit into an OutSystems app via the Model API. Do exactly this unit and nothing else; do not refactor unrelated screens. On every rendered widget you create, set an HTML attribute data-spec-id="<the component id below>" (OutSystems widget Attributes: Name=data-spec-id, Value=the id) so runtime verification can resolve it exactly; for a data table/list also set data-entity="<EntityName>" on the container and data-row-id on each row. Do NOT add any platform Role to a screen unless this unit is explicitly about auth — keep screens Anonymous. Author DIRECTLY and rely on the final model validation — do NOT run read-only introspection loops to enumerate or 'verify' your own work. In particular, do NOT read a `.Name` PROPERTY off Model-API node/widget/type interfaces: MANY do not expose one and it is a compile error (IMobileWidget, IActionNode, IFlowNode, ITypeSignature, IUIFlowNodeSignature, IIdentifierType, IBasicType all lack `.Name`). If you must name a type use GetInterface().Name; otherwise skip the diagnostic and just author. After authoring, run the model validation and report errors/warnings; do NOT publish (the orchestrator publishes).

Seed this FK-linked entity graph (Supplier, Part, QualificationCase, AuditEvent, ReviewTask) with ONE server action LoadSampleData (create it if absent; if it exists, REPLACE its body with this). Author the entities IN THIS ORDER (parents first) so each child can reference its parent's captured Id:
1. CRITICAL FK RULE — resolve every FK by CAPTURING the parent's Id from its CreateAction RETURN VALUE into a local variable, then setting the child's FK attribute to that local. Do NOT look up parents with an aggregate: at runtime an aggregate does NOT return rows created earlier in the SAME action, so a lookup yields empty and the child is skipped. The captured-Id locals are named Supplier_<naturalkey>_Id etc.
2. Each entity gets its OWN INDEPENDENT emptiness guard, in sequence (not nested), so re-runs are idempotent and a later entity still seeds if an earlier one is already populated:
   - INDEPENDENT guard: aggregate (max 1) — if Supplier has 0 rows, seed these (each via a typed local Supplier + one Assign per attribute, then Supplier.CreateAction):
      • Supplier { Code = "ACM"; Name = "Acme Drivetrains"; Tier = "T1 · CRITICAL"; Status = "QUALIFIED"; Country = "USA" } -> Supplier.CreateAction, then CAPTURE its returned Id into local Supplier_acm_Id
      • Supplier { Code = "NVL"; Name = "Novalite Cells"; Tier = "T1 · CRITICAL"; Status = "IN REVIEW"; Country = "USA" } -> Supplier.CreateAction, then CAPTURE its returned Id into local Supplier_nvl_Id
      • Supplier { Code = "BRG"; Name = "Bright Composites"; Tier = "T2 · STANDARD"; Status = "SCREENING"; Country = "MEX" } -> Supplier.CreateAction, then CAPTURE its returned Id into local Supplier_brg_Id
      • Supplier { Code = "TORQ"; Name = "TorqWorks"; Tier = "T2 · STANDARD"; Status = "QUALIFIED"; Country = "CAN" } -> Supplier.CreateAction, then CAPTURE its returned Id into local Supplier_torq_Id
   - INDEPENDENT guard: aggregate (max 1) — if Part has 0 rows, seed these (each via a typed local Part + one Assign per attribute, then Part.CreateAction):
      • Part { Sku = "BRK-014"; Name = "Regen Brake Module"; SupplierId = the captured local Supplier_acm_Id (the Supplier whose Code="ACM"); Category = "Braking"; Status = "PENDING" } -> Part.CreateAction, then CAPTURE its returned Id into local Part_brk-014_Id
      • Part { Sku = "CEL-221"; Name = "4695 Cell Pack"; SupplierId = the captured local Supplier_nvl_Id (the Supplier whose Code="NVL"); Category = "Battery"; Status = "IN REVIEW" } -> Part.CreateAction, then CAPTURE its returned Id into local Part_cel-221_Id
      • Part { Sku = "PNL-071"; Name = "Frunk Panel"; SupplierId = the captured local Supplier_brg_Id (the Supplier whose Code="BRG"); Category = "Body"; Status = "BLOCKED" } -> Part.CreateAction, then CAPTURE its returned Id into local Part_pnl-071_Id
   - INDEPENDENT guard: aggregate (max 1) — if QualificationCase has 0 rows, seed these (each via a typed local QualificationCase + one Assign per attribute, then QualificationCase.CreateAction):
      • QualificationCase { CaseNo = "Q-1041"; SupplierId = the captured local Supplier_acm_Id (the Supplier whose Code="ACM"); PartId = the captured local Part_brk-014_Id (the Part whose Sku="BRK-014"); Tier = "T1 · CRITICAL"; Status = "IN REVIEW"; Stage = "Functional Review"; Score = "92"; SlaState = "OVERDUE"; Owner = "Devin Lang" } -> QualificationCase.CreateAction, then CAPTURE its returned Id into local QualificationCase_q-1041_Id
      • QualificationCase { CaseNo = "Q-1042"; SupplierId = the captured local Supplier_nvl_Id (the Supplier whose Code="NVL"); PartId = the captured local Part_cel-221_Id (the Part whose Sku="CEL-221"); Tier = "T1 · CRITICAL"; Status = "SCREENING"; Stage = "Screening"; Score = "—"; SlaState = "IN REVIEW"; Owner = "Mara Vance" } -> QualificationCase.CreateAction, then CAPTURE its returned Id into local QualificationCase_q-1042_Id
      • QualificationCase { CaseNo = "Q-1043"; SupplierId = the captured local Supplier_brg_Id (the Supplier whose Code="BRG"); PartId = the captured local Part_pnl-071_Id (the Part whose Sku="PNL-071"); Tier = "T2 · STANDARD"; Status = "APPROVED"; Stage = "Activation"; Score = "88"; SlaState = "CLEAR"; Owner = "Sanjay Rao" } -> QualificationCase.CreateAction, then CAPTURE its returned Id into local QualificationCase_q-1043_Id
      • QualificationCase { CaseNo = "Q-1044"; SupplierId = the captured local Supplier_torq_Id (the Supplier whose Code="TORQ"); PartId = the captured local Part_brk-014_Id (the Part whose Sku="BRK-014"); Tier = "T2 · STANDARD"; Status = "IN REVIEW"; Stage = "Qualification"; Score = "76"; SlaState = "IN REVIEW"; Owner = "Devin Lang" } -> QualificationCase.CreateAction, then CAPTURE its returned Id into local QualificationCase_q-1044_Id
   - INDEPENDENT guard: aggregate (max 1) — if AuditEvent has 0 rows, seed these (each via a typed local AuditEvent + one Assign per attribute, then AuditEvent.CreateAction):
      • AuditEvent { CaseId = the captured local QualificationCase_q-1041_Id (the QualificationCase whose CaseNo="Q-1041"); Description = "Case created from supplier intake"; Actor = "System"; At = "Jul 1" } -> AuditEvent.CreateAction
      • AuditEvent { CaseId = the captured local QualificationCase_q-1041_Id (the QualificationCase whose CaseNo="Q-1041"); Description = "Denied-party screening cleared"; Actor = "ScreeningAgent"; At = "Jul 1" } -> AuditEvent.CreateAction
      • AuditEvent { CaseId = the captured local QualificationCase_q-1041_Id (the QualificationCase whose CaseNo="Q-1041"); Description = "Qualification score 92 recorded"; Actor = "Devin Lang"; At = "Jul 3" } -> AuditEvent.CreateAction
      • AuditEvent { CaseId = the captured local QualificationCase_q-1041_Id (the QualificationCase whose CaseNo="Q-1041"); Description = "Procurement review approved"; Actor = "Mara Vance"; At = "Jul 3" } -> AuditEvent.CreateAction
   - INDEPENDENT guard: aggregate (max 1) — if ReviewTask has 0 rows, seed these (each via a typed local ReviewTask + one Assign per attribute, then ReviewTask.CreateAction):
      • ReviewTask { CaseId = the captured local QualificationCase_q-1041_Id (the QualificationCase whose CaseNo="Q-1041"); Team = "Procurement"; State = "APPROVED"; DueDate = "2026-07-02" } -> ReviewTask.CreateAction
      • ReviewTask { CaseId = the captured local QualificationCase_q-1041_Id (the QualificationCase whose CaseNo="Q-1041"); Team = "Quality"; State = "IN REVIEW"; DueDate = "2026-07-09" } -> ReviewTask.CreateAction
      • ReviewTask { CaseId = the captured local QualificationCase_q-1041_Id (the QualificationCase whose CaseNo="Q-1041"); Team = "Engineering"; State = "IN REVIEW"; DueDate = "2026-07-09" } -> ReviewTask.CreateAction
      • ReviewTask { CaseId = the captured local QualificationCase_q-1041_Id (the QualificationCase whose CaseNo="Q-1041"); Team = "Compliance"; State = "PENDING"; DueDate = "2026-07-11" } -> ReviewTask.CreateAction
3. Call LoadSampleData as the FIRST node of the OnReady screen action of dashboard (create the OnReady if absent) so the graph is present on first load.
Make LoadSampleData NON-PUBLIC (a public entity-writer trips OS-DPL-50205). Keep each row's Assigns minimal. After authoring, run validation and confirm change_applied. Do not publish (orchestrator publishes). At runtime, every listed entity — including the FK children — must have its rows.